{
  "schemaVersion": 1,
  "candidate": "stage-10-openzeppelin-5-6-1",
  "inventoryChange": "none",
  "expectedTestCount": 1,
  "expectedCaseCount": 5,
  "sourcePath": "test/solidity/fuzz/WrapperFuzz.t.sol",
  "sourceSha256": "ecd22eec5c14af50f0837f45b8ec72714729bf449645d82752ab430b7b4a8795",
  "names": [
    "test/solidity/fuzz/WrapperFuzz.t.sol:WrapperFuzzTest:testFuzz_wrappedSafeTransferHonorsReceiverAndPreservesStateOnRejection"
  ],
  "cases": [
    {
      "id": "eoa",
      "expected": "The receiver callback is skipped, the transfer succeeds, approval is cleared, balances and ownership update once, and exactly one ordered Transfer log is committed."
    },
    {
      "id": "valid-receiver",
      "expected": "The callback receives the exact token caller, operator, from, token id, and data; the transfer succeeds and approval is cleared."
    },
    {
      "id": "incorrect-return-value",
      "expected": "The exact project-owned ERC721 receiver Error(string) is returned and ownership, balances, approval, and committed logs roll back."
    },
    {
      "id": "reverting-receiver",
      "expected": "Non-empty receiver revert data is bubbled exactly, empty receiver revert data maps to the project-owned ERC721 receiver Error(string), and state and logs roll back."
    },
    {
      "id": "construction-time-code-length",
      "expected": "A receiver under construction has zero code length, the callback is skipped while minting or transferring to it, and a later self-transfer invokes the callback exactly once."
    }
  ]
}
